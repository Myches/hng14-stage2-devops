
# Redis password - change this to a strong random value in production
REDIS_PASSWORD=Mike0502489254

# API URL for the frontend to reach the API (used in docker-compose)
API_URL=http://api:8000

# Application environment
APP_ENV=production
